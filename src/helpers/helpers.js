
/** 
* cleanString
* It will return a clean string. Trimmed and lowercased.
* @param {String} str string that will be clean
* @return {String} clean string
*/
export const cleanString = (str) => {
    return str.toLowerCase().trim();
}
